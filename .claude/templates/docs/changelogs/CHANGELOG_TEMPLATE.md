# <Domain> Changelog

> One-line scope description. Which sidebar screen(s) does this domain own?

## Owns

- **Routes**: `<source path(s) under src/routes/ that belong to this domain>`
- **Components**: `<source path(s) under src/components/ specific to this domain, if any>`
- **Mock data**: `<export name(s) in src/lib/mock.ts this domain reads/writes>`

## Surface

- **Sidebar entry**: `<menu label from config.md's Sidebar/menu section>` → `/<route>`
- **Spec sections**: `<SRS FR-xxx / API Specification §N covering this screen's fields>`

## Timeline

> Newest entries first.

### YYYY-MM-DD — <Short title> (<commit short SHA>)
**Added | Changed | Fixed | Removed.** One-sentence description.

- Optional details (bullets).
- Source: `<path/to/file:line>`.

### YYYY-MM-DD — <Earlier title> (<commit short SHA>)
**Added.** …

---

## Open questions / known gaps

(Optional.) Example:

- Real backend not wired — this domain's data is mock-only until the Router Engine exists
  (see `../../CLAUDE.md`).
